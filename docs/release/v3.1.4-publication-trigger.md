# Asterfold 3.1.4 publication trigger

This audit marker records the owner-authorized GitHub Release publication for version 3.1.4.

The release is produced only by `.github/workflows/release.yml`, which re-runs the complete release gate, publishes the deterministic artifacts, verifies the downloaded release, and marks it as Latest.
