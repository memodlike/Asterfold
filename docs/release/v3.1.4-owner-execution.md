# Asterfold 3.1.4 owner-authorized publication

Repository owner authorization was explicitly confirmed on 2026-08-05.

This marker synchronizes PR #47 after the trusted `pull_request_target` publisher was installed in `main`. The workflow builds from the exact base SHA, runs Linux and Windows release gates, publishes `v3.1.4` as Latest, redownloads all seven assets, and verifies checksums, provenance, and attestations.
