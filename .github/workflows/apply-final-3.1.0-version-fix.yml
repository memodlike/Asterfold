name: Apply final Asterfold 3.1.0 test version fix

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: write

concurrency:
  group: apply-final-asterfold-3-1-0-version-fix
  cancel-in-progress: false

jobs:
  apply:
    if: ${{ github.head_ref == 'perf/windows11-compat-glass-3.1.0' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: perf/windows11-compat-glass-3.1.0
          fetch-depth: 0

      - name: Apply final reviewed test fix once
        shell: bash
        run: |
          set -euo pipefail
          patch=.github/patches/fix-final-3.1.0-version.patch
          if git apply --check "$patch"; then
            git apply --index --whitespace=error "$patch"
            git diff --cached --check
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git commit -m 'test: align backup preview version with 3.1.0'
            git push origin HEAD:perf/windows11-compat-glass-3.1.0
          else
            echo 'Final version assertion patch is already applied.'
          fi
