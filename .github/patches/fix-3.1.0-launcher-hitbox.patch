diff --git a/src/styles/global.css b/src/styles/global.css
index b9dfef1..529c617 100644
--- a/src/styles/global.css
+++ b/src/styles/global.css
@@ -56,7 +56,7 @@ button:focus-visible, input:focus-visible, textarea:focus-visible, select:focus-
 .context-menu__group button i, .context-menu__group button svg { width: 14px; flex: 0 0 14px; }
 .drag-overlay { max-width: 260px; padding: 10px 13px; overflow: hidden; border: 1px solid rgb(255 255 255 / .18); border-radius: 13px; color: #fff; background: #24262a; box-shadow: 0 12px 28px rgb(0 0 0 / .26); text-overflow: ellipsis; white-space: nowrap; font-size: 13px; }
 
-.app-launcher { position: fixed; z-index: 60; left: 18px; bottom: 16px; display: grid; justify-items: start; gap: 8px; }
+.app-launcher { position: fixed; z-index: 60; left: 18px; bottom: 16px; display: grid; width: 116px; justify-items: start; gap: 8px; }
 .launcher-discovery { position: relative; width: min(286px, calc(100vw - 36px)); padding: 15px 40px 14px 15px; border: 1px solid var(--color-border); border-radius: 16px; color: var(--color-text); background: rgb(var(--surface-rgb) / .94); box-shadow: 0 18px 52px rgb(0 0 0 / .20); backdrop-filter: blur(20px) saturate(1.08); }
 .launcher-discovery::after { content: ""; position: absolute; left: 17px; bottom: -7px; width: 12px; height: 12px; border-right: 1px solid var(--color-border); border-bottom: 1px solid var(--color-border); background: inherit; transform: rotate(45deg); }
 .launcher-discovery strong { display: block; margin-bottom: 4px; font-size: 13px; font-weight: 680; }
@@ -64,12 +64,14 @@ button:focus-visible, input:focus-visible, textarea:focus-visible, select:focus-
 .launcher-discovery__dismiss { position: absolute; top: 7px; right: 7px; display: grid; width: 28px; height: 28px; place-items: center; border-radius: 9px; color: var(--color-text-secondary); background: transparent; font-size: 19px; line-height: 1; }
 .launcher-discovery__dismiss:hover, .launcher-discovery__dismiss:focus-visible { color: var(--color-text); background: rgb(var(--border-rgb) / .08); }
 .launcher-discovery__action { min-height: 32px; padding: 6px 10px; border-radius: 9px; color: var(--color-accent-contrast); background: var(--color-accent); font-size: 12px; font-weight: 650; }
-.launcher-trigger { display: flex; align-items: center; width: 116px; height: 38px; gap: 9px; padding: 7px; overflow: hidden; clip-path: inset(0 78px 0 0 round 14px); border: 1px solid transparent; border-radius: 14px; opacity: .34; color: var(--color-text); background: transparent; white-space: nowrap; transition: clip-path 170ms cubic-bezier(.2,.8,.2,1), opacity 130ms ease, background 130ms ease, border-color 130ms ease; }
-.launcher-trigger img { width: 23px; height: 23px; flex: 0 0 23px; }
+.launcher-trigger { position: relative; display: flex; align-items: center; width: 38px; height: 38px; gap: 9px; padding: 7px; overflow: visible; border: 0; border-radius: 14px; opacity: .34; color: var(--color-text); background: transparent; white-space: nowrap; transition: opacity 130ms ease; }
+.launcher-trigger::before { content: ""; position: absolute; inset: 0 auto 0 0; width: 116px; pointer-events: none; border: 1px solid transparent; border-radius: 14px; background: transparent; transform: scaleX(.327586); transform-origin: left center; transition: transform 170ms cubic-bezier(.2,.8,.2,1), background 130ms ease, border-color 130ms ease; }
+.launcher-trigger img { position: relative; z-index: 1; width: 23px; height: 23px; flex: 0 0 23px; }
 [data-theme="dark"] .launcher-trigger img { filter: invert(1); }
-.launcher-trigger span { padding-right: 6px; font-size: 13px; font-weight: 590; opacity: 0; transition: opacity 150ms ease 30ms; }
-.app-launcher:hover .launcher-trigger, .launcher-trigger:focus-visible, .app-launcher.is-open .launcher-trigger { clip-path: inset(0 round 14px); opacity: .88; border-color: var(--color-border); background: rgb(var(--surface-rgb) / .58); backdrop-filter: blur(18px); }
-.app-launcher:hover .launcher-trigger span, .launcher-trigger:focus-visible span, .app-launcher.is-open .launcher-trigger span { opacity: 1; }
+.launcher-trigger span { position: absolute; z-index: 1; left: 38px; width: 78px; padding-right: 6px; pointer-events: auto; font-size: 13px; font-weight: 590; opacity: 0; transform: translateX(-8px); transition: opacity 150ms ease 30ms, transform 170ms cubic-bezier(.2,.8,.2,1); }
+.app-launcher:hover .launcher-trigger, .launcher-trigger:focus-visible, .app-launcher.is-open .launcher-trigger { opacity: .88; }
+.app-launcher:hover .launcher-trigger::before, .launcher-trigger:focus-visible::before, .app-launcher.is-open .launcher-trigger::before { transform: scaleX(1); border-color: var(--color-border); background: rgb(var(--surface-rgb) / .58); backdrop-filter: blur(18px); }
+.app-launcher:hover .launcher-trigger span, .launcher-trigger:focus-visible span, .app-launcher.is-open .launcher-trigger span { opacity: 1; transform: none; }
 .launcher-menu { display: grid; width: 220px; padding: 7px; border: 1px solid var(--color-border); border-radius: 16px; background: rgb(var(--surface-rgb) / .82); box-shadow: 0 22px 64px rgb(0 0 0 / .28), inset 0 1px 0 var(--glass-highlight); backdrop-filter: blur(24px) saturate(1.12); transform-origin: left bottom; animation: launcher-in 190ms cubic-bezier(.2,.8,.2,1) both; }
 .launcher-menu > button { display: flex; align-items: center; min-height: 36px; gap: 10px; padding: 7px 10px; border-radius: 10px; text-align: left; background: transparent; font-size: 13px; }
 .launcher-menu > button:hover, .launcher-menu > button:focus-visible, .launcher-menu > button.is-active { background: rgb(var(--border-rgb) / .09); }
@@ -212,9 +214,9 @@ html[data-performance="software"] :is(.modal, .launcher-menu, .launcher-discover
   -webkit-backdrop-filter: none;
   box-shadow: inset 0 1px 0 var(--glass-highlight), 0 12px 34px rgb(0 0 0 / .20);
 }
-html[data-performance="compatibility"] .launcher-trigger,
-html[data-performance="software"] .launcher-trigger,
-.low-power-mode .launcher-trigger { backdrop-filter: none !important; -webkit-backdrop-filter: none !important; }
+html[data-performance="compatibility"] .launcher-trigger::before,
+html[data-performance="software"] .launcher-trigger::before,
+.low-power-mode .launcher-trigger::before { backdrop-filter: none !important; -webkit-backdrop-filter: none !important; }
 html[data-performance="software"] :is(.board, .modal, .launcher-menu, .launcher-discovery, .context-menu, .empty-state, .bulk-toolbar, .toast) {
   background-color: rgb(var(--surface-rgb) / .96);
   box-shadow: inset 0 1px 0 var(--glass-highlight), 0 6px 18px rgb(0 0 0 / .16);
